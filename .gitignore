.gradle/
build/
local.properties
.idea/
*.iml
.DS_Store
captures/
.kotlin/

# Release signing (Task 9): the real keystore and its credentials never live in the repo.
keystore.properties
*.jks
*.keystore

# Signed release APKs produced by :app:packageReleaseApk.
dist/

# Working notes from the spec-driven development runs: briefs, reports and review diffs. The
# design spec and the plan live in docs/ and are tracked; this is the scaffolding around them.
.superpowers/
